# ============================================================================
# CryptoApp — Dependencias Python 3.11+
# ============================================================================
# Stack de datos, análisis técnico, ML, DL, y utilidades
#
# Versiones fijadas con "==" a lo resuelto y verificado en CI (pytest, ruff,
# mypy en verde) en vez de límites abiertos ">=": una nueva versión mayor de
# cualquiera de estas libs puede romper el pipeline sin previo aviso, y es el
# hallazgo de seguridad que marca SonarCloud sobre dependencias sin fijar.

# --- Datos y Análisis Técnico ---
pandas==3.0.5
numpy==2.4.6
ta==0.11.0                   # Librería de indicadores técnicos (SMA, EMA, RSI, MACD, etc.)

# --- Machine Learning Clásico ---
scikit-learn==1.9.0
xgboost==3.2.0
lightgbm==4.7.0
joblib==1.5.3                # Serialización de modelos .pkl

# --- Deep Learning (con constraint Python 3.11) ---
tensorflow==2.21.0           # Para AITraderStrategy con Keras

# --- Binance API ---
python-binance==1.0.37       # Cliente oficial de Binance

# --- Optimización de Hiperparámetros ---
optuna==4.9.0

# --- WebSocket (Paper Trading en tiempo real) ---
websockets==17.0.1
aiohttp==3.14.3

# --- Serialización de datos ---
msgpack==1.2.1
python-dotenv==1.2.2

# --- Calidad de Código y Testing ---
pytest==9.1.1
pytest-cov==7.1.0
hypothesis==6.165.2          # Tests de propiedades sobre el motor de backtest
ruff==0.16.2                 # Linting y formatting (fijado: el set de reglas por defecto varía entre versiones)
mypy==2.3.0

# --- Validación de Datos ---
pandera==0.32.1              # Contratos de esquema para DataFrames OHLCV

# --- Inspección de Experimentos ---
optuna-dashboard==0.20.0     # UI sobre results/optuna/studies.db

# --- Utilitarios ---
python-dateutil==2.9.0.post0
pytz==2026.3.post1
requests==2.34.2
